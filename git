yum -y update

yum -y install git

git init

echo "# 리포지토리이름" >> test.MD

git add test.MD

git commit -m "Test GitHub"

git remote add origin https://github.com/TensorMSA/TensorMSA.git

git push -u origin master

GitHub 계정이름을 입력해주세요.

GitHub 계정의 비밀번호를 입력해주세요.

저장소에 있던 파일이 GitHub에 올라갔습니다.

GitHub에 접속하셔서 리포지토리에 접속해보시면 test.MD라는 파일이 만들어진것을 확인하실수 있습니다.

git ignore setup
$ git rm -r --cached .
$ git add .
$ git commit -m "fixed untracked files”
